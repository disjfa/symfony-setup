{% extends 'base.html.twig' %}

{% block title %}GrumpyPuppy{% endblock %}

{% block body %}
    <h1>GrumpyPuppy</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ grumpy_puppy.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ grumpy_puppy.name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ grumpy_puppy.description }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('grumpy_puppy_index') }}">back to list</a>

    <a href="{{ path('grumpy_puppy_edit', {'id': grumpy_puppy.id}) }}">edit</a>

    {{ include('grumpy_puppy/_delete_form.html.twig') }}
{% endblock %}
